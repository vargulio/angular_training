import { IfPermissionDirective } from './if-permission.directive';

describe('IfPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new IfPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
